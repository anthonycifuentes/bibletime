{
  "name": "desktop",
  "version": "0.0.1",
  "private": true,
  "description": "BibleTime \u2014 a local-first presentation app for churches and ministries.",
  "author": "Anthony Cifuentes",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "concurrently -k -n web,electron -c blue,green \"pnpm --filter web dev\" \"wait-on http://localhost:3000 && pnpm run build && electron .\"",
    "build": "tsc -p tsconfig.json && tsc src/preload.ts --ignoreConfig --outDir dist --module commonjs --moduleResolution bundler --target es2022 --skipLibCheck --types node && mv dist/preload.js dist/preload.cjs",
    "start": "electron .",
    "typecheck": "tsc --noEmit",
    "package": "pnpm --filter web build && pnpm run build && electron-builder --config electron-builder.yml"
  },
  "devDependencies": {
    "@types/node": "^22",
    "concurrently": "^10.0.4",
    "electron": "^43.2.0",
    "electron-builder": "^25.1.8",
    "typescript": "^6",
    "wait-on": "^9.1.0"
  }
}
